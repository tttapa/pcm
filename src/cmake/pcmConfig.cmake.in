@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
if (@PCM_WITH_SIMDJSON@)
    find_dependency(simdjson)
endif()
if (@PCM_WITH_PUGIXML@)
    find_dependency(pugixml)
endif()
if (@PCM_WITH_OPENSSL@)
    find_dependency(OpenSSL)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/pcmTargets.cmake")

check_required_components(pcm)
